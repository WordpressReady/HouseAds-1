apply plugin: 'com.android.application'

android {
    compileSdkVersion 26

    defaultConfig {
        applicationId "com.lazygeniouz.houseAds.sample"
        minSdkVersion 14
        //noinspection OldTargetApi
        targetSdkVersion 26
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])

    implementation 'com.android.support:appcompat-v7:26.1.0'
    implementation 'com.android.support:cardview-v7:26.1.0'
    //implementation 'com.lazygeniouz:houseAds:1.2'
    implementation project(':library')
}

//20190703: FZSM currently there is the https://mvnrepository.com/artifact/com.lazygeniouz/houseAds version 1.4
//Does have sense to include the library?
//How do I convert this as my own library with version? Currently this version is 1.3

//Latest is in https://github.com/ItzNotABug/HouseAds/blob/master/README.md